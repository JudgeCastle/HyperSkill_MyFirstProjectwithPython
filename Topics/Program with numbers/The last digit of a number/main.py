# Define variable, which takes the users input for a number
user_input = int(input())
# Define the expression which takes users input and finds remainder from the ones position
math_time = user_input % 10
# Gives us our resolution
print(math_time)